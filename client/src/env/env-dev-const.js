const serverURL = 'http://localhost:8080'
const serverPath = ''
const completeServerURL = `${serverURL}${serverPath}`
const clientPath = ``

export default {
    serverURL,
    serverPath,
    completeServerURL,
    clientPath
}