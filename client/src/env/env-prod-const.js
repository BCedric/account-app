const serverURL = 'http://skalimero.alwaysdata.net'
const serverPath = '/test_node'
const completeServerURL = `${serverURL}${serverPath}`
const clientPath = `/comptes`

export default {
    serverURL,
    serverPath,
    completeServerURL,
    clientPath
}